---
import Layout from '@layouts/Layout.astro';
import type { PostMeta } from '@config/seo';

// You can override global SEO settings for this specific page here.
// Example: { title: 'Custom Now Title', description: 'Custom description', robot: false }
const meta: PostMeta = { robot: false };
---

<Layout meta={meta}> now </Layout>
